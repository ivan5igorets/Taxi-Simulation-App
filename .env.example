# Пароль БД для прод-деплоя через docker-compose (только backend/db, не для локальной
# разработки — там backend/.env читается отдельно через npm run dev).
#
# Скопируй в .env на сервере (НЕ в git!) и задай реальный пароль:
#   cp .env.example .env
POSTGRES_PASSWORD=change_me_before_deploy
